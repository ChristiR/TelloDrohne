#from multiprocessing_test import pool
import multiprocessing as Process
import numpy as np

n_proc = Process.cpu_count() #4

def menu():

def stream():

p1 = Process(target=menu)
p2 = Process(target=stream)